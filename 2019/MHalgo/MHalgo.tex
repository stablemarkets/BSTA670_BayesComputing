\documentclass[10pt]{article}

\usepackage[left=24mm,top=.5in,bottom=1in,right=24mm]{geometry}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{subfig}
\usepackage{amssymb}
\usepackage[labelformat=empty]{caption}
\usepackage{layouts}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage[font=small,skip=0pt]{caption}
\usepackage{mathrsfs}
\usepackage{bm}
\usepackage{mathtools}
\usepackage{algorithm2e}
\DontPrintSemicolon 

\DeclareMathOperator*{\argmax}{argmax} % thin space, limits underneath in


\def\inprobHIGH{\,{\buildrel p \over \rightarrow}\,} 
\def\indistHIGH{\,{\buildrel d \over \rightarrow}\,} 

\begin{document}
	
	
\begin{algorithm}[H]
\SetAlgoLined

 \For{t = 1 to T}{
 \;
 $\theta^* \sim N(\theta^{(t-1)}, \tau)$ \;
$\alpha = \frac{ \tilde p (\theta^* | D) }{  \tilde p (\theta^{(t-1)} | D) }$\;
 \;
  $U \sim Ber( p = min(1, \alpha) )$ \;  
  \;
  \eIf{$U==1$}{
    $\theta^{(t)} \leftarrow \theta^*$ \;
   }{
    $\theta^{(t)} \leftarrow \theta^{(t-1)} $\;
  }
 }
\end{algorithm}





\end{document}